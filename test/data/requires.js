require('a')
var foo = require('b').b.c.d().e.f().g
if (foobar) {
  var bla = function() {
    switch (baz) {
      default:
        require('c')
    }
  }
  qux(1, 3, quux(require('d').bar.foo, 4), 2)
}

function func() {
  return
}
